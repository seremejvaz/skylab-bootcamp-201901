extends ../layout

block content
    include ../search/index
        section.tracks-list
             each track in tracks
                a(href=`/play-track&${track.id}`)
                        div
                            h3=track.name
        +search 